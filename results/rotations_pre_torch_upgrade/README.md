Per-rotation records produced under torch 2.0.1+cu117, before the shared
environment was upgraded to 2.7.1+cu126 on 2026-08-11 (REVIEWER_PROOFING §13).

These seeds were re-run under 2.7.1 so that every result cell is single-version;
the re-run overwrote the live records in ../rotations/. Copies are kept here so
the pre/post comparison stays auditable rather than being destroyed by its own
remedy — the same mistake environment.json made.
